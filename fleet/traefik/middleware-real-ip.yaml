apiVersion: traefik.io/v1alpha1
kind: Middleware
metadata:
  name: real-ip
  namespace: kube-system
spec:
  headers:
    customRequestHeaders:
      X-Forwarded-For: "{{ client_ip }}"
      X-Real-IP: "{{ client_ip }}"
